/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['opis-cdn.tinkoffjournal.ru', 'img-cdn.tinkoffjournal.ru'],
  },
}

export default nextConfig
